# morrow environment variables.
# copy to .env and fill in. never commit .env. secrets live here and only here.
# non-secret tunables have defaults in packages/config/config.ts; env vars
# listed here override them per deployment.

# ---------------------------------------------------------------------------
# chain (verified defaults live in config.ts; override per deployment)
# ---------------------------------------------------------------------------

# robinhood chain id. defaults to 4663. leave blank to use the default.
MORROW_CHAIN_ID=

# robinhood chain https json-rpc endpoint. defaults to the public endpoint
# https://rpc.mainnet.chain.robinhood.com, which is rate limited. for
# production and for pool discovery, set this to an alchemy or quicknode
# robinhood chain url. do not run the indexer against the public endpoint.
MORROW_RPC_URL=

# block explorer base url. defaults to https://robinhoodchain.blockscout.com.
MORROW_EXPLORER_URL=

# blockscout verifier base for forge contract verification. defaults to
# https://robinhoodchain.blockscout.com/api/.
MORROW_VERIFIER_URL=

# deployed MorrowCommits contract address. known after the contract deploy
# step in SETUP.md.
MORROW_COMMITS_ADDRESS=

# ---------------------------------------------------------------------------
# database (required)
# ---------------------------------------------------------------------------

# supabase postgres connection string. use the pooled connection string from
# the supabase dashboard (transaction mode) for the api, and the direct
# string for the indexer if you prefer. this is a secret.
DATABASE_URL=

# ---------------------------------------------------------------------------
# publisher (required for onchain commits)
# ---------------------------------------------------------------------------

# hex private key (0x...) of the publisher wallet. must match the publisher
# address set on the MorrowCommits contract. fund with a little eth on
# robinhood chain for gas. this is a secret. never log it.
PUBLISHER_PRIVATE_KEY=

# ---------------------------------------------------------------------------
# contract deploy (used only by foundry, see packages/contracts)
# ---------------------------------------------------------------------------

# deployer private key for forge script. can be the same wallet as the
# publisher for v1. this is a secret.
DEPLOYER_PRIVATE_KEY=

# public address of the publisher wallet, passed to the constructor at
# deploy time. derives from PUBLISHER_PRIVATE_KEY.
PUBLISHER_ADDRESS=

# ---------------------------------------------------------------------------
# api
# ---------------------------------------------------------------------------

API_PORT=8080
API_HOST=0.0.0.0

# single origin or * for the dashboard during development.
API_CORS_ORIGIN=*

# bearer token protecting the admin endpoints (anchor inserts, key creation).
# generate something long and random. this is a secret.
ADMIN_TOKEN=

# enable the x402 payment-required path on price endpoints. settlement
# verification is a stub interface until wired (see apps/api/src/x402.ts).
X402_ENABLED=false

# ---------------------------------------------------------------------------
# indexer
# ---------------------------------------------------------------------------

# poll interval in ms (default 30000) and cycle length in seconds (default
# 600). defaults live in config.ts; override here per environment.
MORROW_POLL_MS=
MORROW_CYCLE_SECONDS=
MORROW_TWAP_WINDOW_SECONDS=

# run the whole pipeline against synthetic pools and proxies. lets you boot
# indexer, api, and web before any placeholder is filled.
MOCK_MODE=false

# eth/usd source for dollarizing weth-quoted pools. only fetched when a
# tracked token uses a weth pool (none in the default launch set). set the
# endpoint and json path when enabling a weth token; staleness in ms gates
# whether a weth token is skipped (degrades confidence) vs dollarized.
MORROW_ETHUSD_URL=
MORROW_ETHUSD_JSONPATH=
MORROW_ETHUSD_STALENESS_MS=

# anchor automation. off by default; the manual admin endpoints work either
# way. when true, the indexer inserts close and open anchors on schedule from
# the sources in config (anchors.sources, wire their urls and json path).
# delays are minutes after the official close/open; missed-hours is the ops
# alert deadline.
MORROW_ANCHOR_AUTOMATED=false
MORROW_ANCHOR_CLOSE_DELAY_MIN=
MORROW_ANCHOR_OPEN_DELAY_MIN=
MORROW_ANCHOR_MISSED_HOURS=
MORROW_ANCHOR_STALE_GRACE_MIN=

# ops alert cooldown in ms: how often the same condition may re-notify.
MORROW_OPS_COOLDOWN_MS=

# ops thresholds (defaults in config.ts; override per deployment).
MORROW_OPS_HEARTBEAT_STALE_CYCLES=
MORROW_OPS_RPC_FAILURE_TICKS=
MORROW_OPS_PUBLISHER_FLOOR_ETH=
MORROW_OPS_GAS_PER_COMMIT_ETH=
MORROW_OPS_5XX_WINDOW_MS=
MORROW_OPS_5XX_THRESHOLD=
MORROW_OPS_MONITOR_INTERVAL_MS=

# ---------------------------------------------------------------------------
# telegram: public divergence alerts (@morrow/telegram worker)
# ---------------------------------------------------------------------------

# absolute spread percent that triggers a public alert, and the per-token
# cooldown and poll interval. defaults 2 / 30m / 60s.
MORROW_TG_THRESHOLD_PCT=
MORROW_TG_COOLDOWN_MS=
MORROW_TG_POLL_MS=

# dashboard base url used in the token link inside alert messages.
MORROW_PUBLIC_WEB_URL=

# public channel bot token and chat id. secrets. env only.
TELEGRAM_PUBLIC_BOT_TOKEN=
TELEGRAM_PUBLIC_CHAT_ID=

# log instead of send. on by default until the token is set.
TELEGRAM_DRY_RUN=true

# ---------------------------------------------------------------------------
# telegram: private ops channel (indexer + api pages)
# ---------------------------------------------------------------------------

# separate token and chat from the public channel. secrets. env only.
TELEGRAM_OPS_BOT_TOKEN=
TELEGRAM_OPS_CHAT_ID=

# log instead of send. on by default until the token is set.
TELEGRAM_OPS_DRY_RUN=true

# ---------------------------------------------------------------------------
# weekly accuracy receipts
# ---------------------------------------------------------------------------

# generate the weekly receipt automatically in the worker (default true), on
# this weekday in America/New_York (1 = monday), this many minutes after the
# 09:30 open so the open anchors have landed.
MORROW_RECEIPTS_AUTO=
MORROW_RECEIPTS_WEEKDAY=
MORROW_RECEIPTS_AFTER_OPEN_MIN=

# ---------------------------------------------------------------------------
# pool discovery (multi-protocol: uniswap v2 + v3 + v4)
# ---------------------------------------------------------------------------

# run a discovery pass weekly in the worker (default true), on this weekday in
# America/New_York (1 = monday) at this hour. every run is recorded in the
# pool_discovery_runs table; the worker alerts, it never edits config.
MORROW_DISCOVERY_AUTO=
MORROW_DISCOVERY_WEEKDAY=
MORROW_DISCOVERY_HOUR_ET=

# plausibility gate: a pool whose per-share price deviates more than this
# fraction from the latest close anchor is flagged implausible and never
# selected (default 0.25). a pool below this dollar depth counts as empty
# (default 1).
MORROW_DISCOVERY_PLAUSIBILITY=
MORROW_DISCOVERY_EMPTY_DEPTH_USD=

# drying-up alert: a configured pool whose dollar depth stays below this floor
# (default 500) for this many consecutive runs (default 3) raises an ops alert.
MORROW_DISCOVERY_DEPTH_ALERT_USD=
MORROW_DISCOVERY_DEPTH_ALERT_RUNS=

# ---------------------------------------------------------------------------
# web (vercel)
# ---------------------------------------------------------------------------

# public base url of the morrow api, e.g. https://api.morrow.example
NEXT_PUBLIC_API_URL=

# public explorer base url for tx links. defaults to the blockscout url.
NEXT_PUBLIC_EXPLORER_URL=
